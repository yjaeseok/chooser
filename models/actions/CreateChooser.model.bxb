action (CreateChooser) {
  type (Search)
  description (Create Initial Chooser)
  collect {
    input (numPlayer) {
      type (NumPlayerConcept)
      min (Required) max (One)
      description (Try to create chooser)
    }
  }
  output (Chooser)
}
