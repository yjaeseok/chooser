action (ChoosePlayer) {
  type (Search)
  description (Choose Player)
  collect {
    input (playerAction) {
      type (PlayerActionConcept)
      min (Required) max (One)
    }
    input (chooser) {
      type (Chooser)
      min (Required)
      max (One)
    }
  }
  output (Chooser)
}
